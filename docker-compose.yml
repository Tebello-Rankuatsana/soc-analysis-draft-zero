version: '3.8'

services:
  soc-analysis:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/app/uploads 
    environment:
      - FLASK_ENV=development
      - FLASK_APP=app.py
    privileged: true
    networks:
      - soc-network

networks:
  soc-network:
    driver: bridge